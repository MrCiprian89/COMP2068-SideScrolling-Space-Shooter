module states {
    export function level2State() {
        states.playState();
    }

    export function play2(): void {
       // player will not replensh lives each level
        // lives = constants.PLAYER_LIVES;
       // healthBar.update(lives);
       // testLabel = new objects.Label("Level 2", 300, 50);
      //  game.addChild(testLabel);




    }

}//END module 